apiVersion: v1
kind: Service
metadata:
   name: http-app
spec:
   selector:
      app: http-app
   ports:
      - name: http
        protocol: TCP
        port: 5000
        targetPort: 5000
   type: ClusterIP